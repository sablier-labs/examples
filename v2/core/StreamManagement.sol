// SPDX-License-Identifier: GPL-3.0-or-later
pragma solidity >=0.8.13;

contract StreamManagement {
    function cancel(uint256 streamId) external pure {
        streamId;
    }

    function renounce(uint256 streamId) external pure {
        streamId;
    }

    function withdraw(uint256 streamId) external pure {
        streamId;
    }
}
